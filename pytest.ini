[pytest]
# Only collect real unit tests. Without `testpaths`, pytest also discovers
# standalone scripts that are NOT pytest tests (scripts/test_phase3.py runs
# the full pipeline at import time and calls sys.exit(), test_video.py,
# config/test_*.py), which breaks collection with INTERNALERROR/hangs.
testpaths = tests
